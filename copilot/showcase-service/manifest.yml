# The manifest for the "showcase-service" service.
# Read the full specification for the "Load Balanced Web Service" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/

# Your service name will be used in naming your resources like log groups, ECS services, etc.
name: showcase-service
type: Load Balanced Web Service

# Distribute traffic to your service.
http:
  # Requests to this path will be forwarded to your service.
  # To match all requests you can use the "/" path.
  path: '/'
  alias: ["showcaseedu.com", "www.showcaseedu.com"]
  # You can specify a custom health check path. The default is "/".
  healthcheck:
    path: '/'
    success_codes: '200-301'

# Configuration for your containers and service.
image:
  # Docker build arguments. For additional overrides: https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/#image-build
  build: Dockerfile
  # Port exposed through your container to route traffic to it.
  port: 8000

cpu: 256       # Number of CPU units for the task.
memory: 512    # Amount of memory in MiB used by the task.
count: 1       # Number of tasks that should be running in your service.
exec: true     # Enable running commands in your container.

# Optional fields for more advanced use-cases.
#
variables:                    # Pass environment variables as key value pairs.
  DJANGO_SETTINGS_MODULE: portfolio.settings.production
  AWS_ACCESS_KEY: AKIAJY3CQJJSV5LRZQGQ
  DJANGO_DEBUG: True
  RDS_DB_NAME: ebdb
  RDS_USERNAME: eli
  RDS_HOSTNAME: showcase-db.cziqh3nilbux.us-west-2.rds.amazonaws.com
  RDS_PORT: 5432
  SES_USERNAME: AKIAUS77QEIR5Q4LG76I
  OAUTHLIB_RELAX_TOKEN_SCOPE: True

secrets:                      # Pass secrets from AWS Systems Manager (SSM) Parameter Store.
#  GITHUB_TOKEN: GITHUB_TOKEN  # The key is the name of the environment variable, the value is the name of the SSM parameter.
  SECRET_KEY: /copilot/showcase-app/test/secrets/SECRET_KEY
  AWS_SECRET_KEY: /copilot/showcase-app/test/secrets/AWS_SECRET_KEY
  RDS_PASSWORD: /copilot/showcase-app/test/secrets/RDS_PASSWORD
  GOOGLE_SECRET: /copilot/showcase-app/test/secrets/GOOGLE_SECRET
  SES_PASSWORD: /copilot/showcase-app/test/secrets/SES_PASSWORD
  GOOGLE_CONFIG: /copilot/showcase-app/test/secrets/GOOGLE_CONFIG
  AZURE_SECRET: /copilot/showcase-app/test/secrets/AZURE_SECRET


# You can override any of the values defined above by environment.
#environments:
#  test:
#    count: 2               # Number of tasks to run for the "test" environment.
